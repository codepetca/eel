{
  "name": "@eel/backend",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "start": "bun src/index.ts",
    "dev": "bun --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["multiplayer", "colyseus", "realtime", "backend"],
  "author": "",
  "license": "MIT",
  "description": "Backend server for multiplayer games using Colyseus",
  "dependencies": {
    "@colyseus/core": "^0.16.18",
    "@colyseus/schema": "^3.0.42",
    "@types/express": "^5.0.3",
    "@types/node": "^24.0.4",
    "colyseus": "^0.16.4",
    "express": "^5.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
